Should be clone recursively in:
`C:\Users\thomashourlier\Documents\projet-couche-installer`

We should build the project from `project-couche-installer\source` by running
`npm i && .\node_modules\.bin\gulp bundle`