Should be clone recursively in:
`C:\Users\thomashourlier\Documents\projet-couche-installer`

We MUST build the project from `project-couche-installer\source` by running
`npm i && .\node_modules\.bin\gulp bundle` before running the innosetup compiler.